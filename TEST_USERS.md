# 🧪 Test Users / المستخدمين التجريبيين

## أغراض الاختبار

تم إنشاء مستخدمين تجريبيين مختلفين لاختبار وظائف الـ Authentication و Role-Based Access Control.

---

## 👥 المستخدمين التجريبيين

### 1️⃣ Super Admin (مدير النظام الأعلى)
```
الإيميل: admin@gahar.sa
الباسورد: Admin@123
الاسم الأول: Super
الاسم الأخير: Admin
الدور: SuperAdmin
الصلاحيات: ✅ كل الصلاحيات
```

**الاستخدام**:
- اختبار إدارة النظام الكاملة
- إدارة المستخدمين والأدوار
- الوصول الكامل لـ Swagger

---

### 2️⃣ Admin عادي (مدير)
```
الإيميل: admin@example.com
الباسورد: Admin@123
الاسم الأول: محمد
الاسم الأخير: علي
الدور: Admin
الصلاحيات: ✅ جميع الصلاحيات ما عدا سجلات التدقيق
```

**الاستخدام**:
- اختبار الوصول للـ Swagger
- إدارة المحتوى والمستخدمين
- إدارة الأدوار والصلاحيات

---

### 3️⃣ Editor (محرر)
```
الإيميل: editor@example.com
الباسورد: Editor@123
الاسم الأول: فاطمة
الاسم الأخير: محمد
الدور: Editor
الصلاحيات: ✅ View, Create, Edit (بدون Delete)
```

**الاستخدام**:
- اختبار صلاحيات محدودة
- إنشاء وتعديل المحتوى
- لا يمكن الوصول لـ Swagger (ليس Admin)

---

### 4️⃣ Viewer (مشاهد)
```
الإيميل: viewer@example.com
الباسورد: Viewer@123
الاسم الأول: أحمد
الاسم الأخير: حسن
الدور: Viewer
الصلاحيات: ✅ View فقط (عرض فقط)
```

**الاستخدام**:
- اختبار صلاحيات محدودة جداً
- عرض المحتوى فقط
- لا يمكن التعديل أو الحذف

---

### 5️⃣ Regular User (مستخدم عادي)
```
الإيميل: user@example.com
الباسورد: User@123
الاسم الأول: سارة
الاسم الأخير: علي
الدور: Viewer
الصلاحيات: ✅ View فقط
```

**الاستخدام**:
- اختبار مستخدم عادي
- صلاحيات المشاهدة فقط

---

## 🧪 الاختبارات المقترحة

### Test 1: Admin يمكنه الوصول لـ Swagger
```bash
# 1. Login كـ Admin
curl -X POST "http://localhost:5000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@example.com","password":"Admin@123"}'

# 2. انسخ ال accessToken
# 3. اذهب لـ http://localhost:5000/swagger/index.html
# 4. اضغط Authorize وأدخل: Bearer <token>
# ✅ يجب أن يعمل
```

### Test 2: Editor لا يمكنه الوصول لـ Swagger
```bash
# 1. Login كـ Editor
curl -X POST "http://localhost:5000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"editor@example.com","password":"Editor@123"}'

# 2. انسخ ال accessToken
# 3. اذهب لـ http://localhost:5000/swagger/index.html
# 4. اضغط Authorize وأدخل: Bearer <token>
# ❌ يجب أن يفشل (403 Forbidden)
```

### Test 3: Viewer يحصل على صلاحيات محدودة
```bash
# 1. Login كـ Viewer
curl -X POST "http://localhost:5000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"viewer@example.com","password":"Viewer@123"}'

# 2. استخدم ال Token للعمليات المختلفة
# ✅ يمكن الـ View
# ❌ لا يمكن الـ Create, Edit, Delete
```

### Test 4: Super Admin له كل الصلاحيات
```bash
# 1. Login كـ Super Admin
curl -X POST "http://localhost:5000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@gahar.sa","password":"Admin@123"}'

# 2. يمكنه الوصول لكل شيء
# ✅ Swagger: يعمل
# ✅ جميع العمليات: تعمل
```

---

## 📊 جدول الصلاحيات

| الدور | Swagger | View | Create | Edit | Delete | Delete Roles |
|------|---------|------|--------|------|--------|---|
| **Super Admin** | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| **Admin** | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ |
| **Editor** | ❌ | ✅ | ✅ | ✅ | ❌ | ❌ |
| **Viewer** | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |
| **User** | ❌ | ✅ | ❌ | ❌ | ❌ | ❌ |

---

## 🔑 سريع Reference

### Swagger Access
```
✅ Super Admin: admin@gahar.sa / Admin@123
✅ Admin: admin@example.com / Admin@123
❌ Editor: editor@example.com / Editor@123
❌ Viewer: viewer@example.com / Viewer@123
❌ User: user@example.com / User@123
```

### Login Command Template
```bash
curl -X POST "http://localhost:5000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"EMAIL","password":"PASSWORD"}'
```

---

## 🚀 كيفية استخدام المستخدمين

### الخطوة 1: تشغيل التطبيق
```bash
cd Gahar_Backend
dotnet run
```

### الخطوة 2: سيتم إنشاء المستخدمين تلقائياً
- عند أول تشغيل، سيتم تشغيل `DataSeeder`
- سيتم إنشاء جميع المستخدمين التجريبيين

### الخطوة 3: جرب الدخول
```bash
# اختر أحد المستخدمين من الجدول أعلاه
curl -X POST "http://localhost:5000/api/auth/login" \
  -H "Content-Type: application/json" \
  -d '{"email":"CHOSEN_EMAIL","password":"CHOSEN_PASSWORD"}'
```

### الخطوة 4: استخدم الـ Token
```bash
# احصل على ال accessToken من الرد
# استخدمه في الـ Headers:
curl http://localhost:5000/api/some-endpoint \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 🔐 أمان المستخدمين التجريبيين

⚠️ **ملاحظة مهمة**:
- هذه المستخدمين **للتطوير والاختبار فقط**
- **لا تستخدمهم في الإنتاج**
- استخدم كلمات مرور قوية في الإنتاج
- يمكنك حذفهم بسهولة من قاعدة البيانات

---

## 🛠️ كيفية حذف المستخدمين التجريبيين

### في قاعدة البيانات SQL:
```sql
-- حذف المستخدمين التجريبيين
DELETE FROM UserRoles 
WHERE UserId IN (
  SELECT Id FROM Users 
  WHERE Email IN (
    'admin@example.com',
    'editor@example.com',
  'viewer@example.com',
    'user@example.com'
  )
);

DELETE FROM Users 
WHERE Email IN (
  'admin@example.com',
  'editor@example.com',
  'viewer@example.com',
  'user@example.com'
);
```

---

## 📝 ملاحظات

1. **كلمات المرور**:
   - جميع المستخدمين لهم نفس صيغة الباسورد: `[RoleName]@123`
   - Super Admin: `Admin@123`
   - Admin: `Admin@123`
   - Editor: `Editor@123`
   - Viewer: `Viewer@123`
   - User: `User@123`

2. **الأسماء**:
   - الأسماء الأولى والأخيرة باللغة العربية
   - يمكن تعديلها في `DataSeeder.cs`

3. **الأدوار**:
   - تم تعيين الأدوار تلقائياً
   - الصلاحيات تُعيَّن بناءً على الدور

4. **التحديث**:
   - إذا غيرت المستخدمين، احذف البيانات وشغل البرنامج مرة أخرى
   - أو استخدم `dotnet ef database drop` و `dotnet ef database update`

---

## ✅ الخطوات التالية

1. ✅ شغّل التطبيق
2. ✅ المستخدمين سيتم إنشاؤهم تلقائياً
3. ✅ اختر مستخدم من الجدول
4. ✅ سجّل الدخول واحصل على Token
5. ✅ استخدم Token للوصول لـ Swagger أو API

---

## 🎯 حالات الاستخدام

### للمطورين
- استخدم **Super Admin** لاختبار كل شيء
- استخدم **Admin** لاختبار الوصول لـ Swagger

### لمختبري الجودة
- استخدم جميع المستخدمين لاختبار الصلاحيات
- اختبر أن كل دور له الصلاحيات الصحيحة

### للمهندسين
- تحقق من أن `SwaggerAuthenticationMiddleware` يعمل بشكل صحيح
- اختبر 401 (Unauthorized) و 403 (Forbidden) responses

---

**تم إنشاء**: January 2024  
**آخر تحديث**: January 2024  
**الحالة**: ✅ جاهز للاستخدام

---

**Happy Testing!** 🧪✨
